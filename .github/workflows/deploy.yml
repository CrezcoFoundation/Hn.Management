name: Deploy

on:
    push:
        branches: [ "develop" ]

jobs:
    web-deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@v3
